# Create a blank git repo somewhere (I use github)

# Clone the repo down to my local drive
git clone <repo url from github>

cd <the folder that was created from git clone>

npm init

npm install express request --save